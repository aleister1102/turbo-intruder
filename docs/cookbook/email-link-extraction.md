# Email Link Extraction

- Parse responses and extract links/emails from bodies (resources/examples/email-link-extraction.py).
- Combine with decorators like `MatchStatus(200)` to focus successful responses.